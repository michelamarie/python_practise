#!/usr/bin/python3

import re

phone_number = input()

def validate_phone(phone_number):
    if phone_number is > len(str(12))
    if phone_number is re.compile(r'\d'):
        return True
    if len(str(phone_number)) < 5:
        return False
    return True

    if True:
        print("You have entered a valid phone number!")
    
    else:
        print("That's not a valid phone number. :(")

 

    #if phone_number:
    #    re.compile(r'^0')
    #    len(str(phone_number)) == 8 or len(str(phone_number)) == 9
    #        print("The area code is the same as that of the caller.")



validate_phone(phone_number)